# Project Context

## Deployment Information
- **Frontend (`sully-frontend` repository)**: Deployed to **Cloudflare (CF)**.
- **Backend (`csyos-workers` repository)**: Deployed to **Cloudflare (CF)**.

*Note: The frontend was previously tracked as Vercel but has been migrated to Cloudflare, using the `sully-frontend` git repository.*
